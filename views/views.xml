<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="cash_box.list">
      <field name="name">cash_box list</field>
      <field name="model">cash_box.cash_box</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="cash_box.action_window">
      <field name="name">cash_box window</field>
      <field name="res_model">cash_box.cash_box</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="cash_box.action_server">
      <field name="name">cash_box server</field>
      <field name="model_id" ref="model_cash_box_cash_box"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="cash_box" id="cash_box.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="cash_box.menu_1" parent="cash_box.menu_root"/>
    <menuitem name="Menu 2" id="cash_box.menu_2" parent="cash_box.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="cash_box.menu_1_list" parent="cash_box.menu_1"
              action="cash_box.action_window"/>
    <menuitem name="Server to list" id="cash_box" parent="cash_box.menu_2"
              action="cash_box.action_server"/>
    -->
  </data>
</odoo>